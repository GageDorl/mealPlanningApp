# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v56.0.0/ before writing any code.

# Project Notes

- This is a **meal planning app** in the boilerplate phase.
- Scaffolded with `create-expo-app`; web support added via `react-native-web`.
- Feature planning/specification has not started yet — will be done with the team (e.g. grill-me or speckit).
- Do not add features or architecture beyond what is explicitly requested.
